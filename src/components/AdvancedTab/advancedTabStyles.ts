import { makeStyles } from '@fluentui/react-components';

/**
 * Minimal AdvancedTab styles - trust FluentUI defaults
 */
export const advancedTabStyles = makeStyles({
  // Only essential component-specific styles
  colorInput: {
    width: '3rem', // ~48px 
    height: '2.5rem', // ~40px
  },
});
